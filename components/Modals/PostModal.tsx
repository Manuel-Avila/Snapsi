import { Modal } from "../ui/Modal";
import { Text, View } from "react-native";

export default function PostModal() {
  return (
    <Modal isVisible={true} onClose={() => {}}>
      <View>
        <Text>fadsfasdfsad</Text>
      </View>
    </Modal>
  );
}
